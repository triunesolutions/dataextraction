pypdf>=4.0
pydantic>=2.6
python-dotenv>=1.0
# The Groq backend uses only the Python standard library (urllib) — no extra package.
# Only needed if you switch MODEL_BACKEND=ollama (fully-local mode):
# ollama>=0.4.0
